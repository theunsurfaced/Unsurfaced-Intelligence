{
 "registry": {
  "SEAM:LEADERBOARD": {
   "file": "arcade/pop-a-shot/index.html",
   "purpose": "spec-3 leaderboard mount in showCard()"
  },
  "SEAM:SIGNUP_ENDPOINT": {
   "file": "arcade/pop-a-shot/index.html",
   "purpose": "beta email capture -> join flow"
  },
  "SEAM:CAMERA_MODE": {
   "file": "arcade/pop-a-shot/index.html",
   "purpose": "MediaPipe hand-tracking mode gate"
  },
  "SEAM:REVEAL_3D": {
   "file": "arcade/claw/index.html",
   "purpose": "hi-res prize object mount in reveal"
  },
  "SEAM:SIGNUP_ENDPOINT@claw": {
   "file": "arcade/claw/index.html",
   "tag": "SEAM:SIGNUP_ENDPOINT",
   "purpose": "win email capture; PLAY/EXCAVATE/MINE crossover attach"
  },
  "SEAM:ACTIVITY_LOG": {
   "file": "worker/src/index.js",
   "purpose": "logEvent() \u2014 every endpoint records activity (0007)"
  },
  "SEAM:MODEL_POOL": {
   "file": "worker/src/index.js",
   "purpose": "callModel() router; t1/t2 bulk public, t3 voice; sensitive pins to t3"
  },
  "SEAM:LEADERBOARD@rps": {
   "file": "arcade/rps/index.html",
   "tag": "SEAM:LEADERBOARD",
   "purpose": "compete streak submit + match-end board card + shared join"
  },
  "SEAM:LEADERBOARD@hub": {
   "file": "arcade/index.html",
   "tag": "SEAM:LEADERBOARD",
   "purpose": "public tabbed board (RPS/CLAW/POP) \u2014 anonymous reads of the spine"
  },
  "SEAM:PREVIEW@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:PREVIEW",
   "purpose": "server-side reader: extract + translate-to-English + edge cache"
  },
  "SEAM:PREVIEW@daily": {
   "file": "daily/index.html",
   "tag": "SEAM:PREVIEW",
   "purpose": "reader overlay, preview-first credits, lazy key visuals"
  },
  "SEAM:PREVIEW@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:PREVIEW",
   "purpose": "reader overlay; EXCAVATE source links preview in-window"
  },
  "SEAM:KNOWLEDGE@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:KNOWLEDGE",
   "purpose": "feed doorway: chunk + embed (own account) + knowledge_base writes, admin-gated"
  },
  "SEAM:KNOWLEDGE@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:KNOWLEDGE",
   "purpose": "FEED panel: paste/link/file in, recall test, recent drops"
  },
  "SEAM:STUDYBOARD@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:STUDYBOARD",
   "purpose": "public shelf read: live + open-audience + opted-in only, safe fields"
  },
  "SEAM:STUDYBOARD@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:STUDYBOARD",
   "purpose": "board overlay, third MINE door, gate affordance, partner listing toggle, take bridge"
  },
  "SEAM:STUDIO@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:STUDIO",
   "purpose": "content engine: manifest cut on cron, voice layer, admin routes, deploy archive"
  },
  "SEAM:STUDIO@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:STUDIO",
   "purpose": "house renderer + download counter: paint-op templates, store-zip, three-ring reveal"
  },
  "SEAM:ARCHIVE@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:ARCHIVE",
   "purpose": "back-issue shelf: public archive index + by-issue reader, published-only"
  },
  "SEAM:ARCHIVE@daily": {
   "file": "daily/index.html",
   "tag": "SEAM:ARCHIVE",
   "purpose": "the stand: folio opens the shelf, deep-linkable ?issue=NN"
  },
  "SEAM:ENDGAME@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:ENDGAME",
   "purpose": "the economy: rotating code, generic match rail, claims + tickets, treasury routes, 7-game registry"
  },
  "SEAM:ENDGAME@arcade-hub": {
   "file": "arcade/index.html",
   "tag": "SEAM:ENDGAME",
   "purpose": "treasury panel (staff-door, shared-origin session) + coming-to-the-floor band"
  },
  "SEAM:INTRO@arcade-hub": {
   "file": "arcade/index.html",
   "tag": "SEAM:INTRO",
   "purpose": "boot sequence: attract video, instant skip, watchdog + reduced-motion dissolve"
  },
  "SEAM:THUMB@cabinet": {
   "file": "arcade/thumb/index.html",
   "tag": "SEAM:THUMB",
   "purpose": "thumb wrestling cabinet: pure match core, drag + camera translators, spine + match rail"
  },
  "SEAM:THUMB@arcade-hub": {
   "file": "arcade/index.html",
   "tag": "SEAM:THUMB",
   "purpose": "cabinet 04 card + board tab"
  },
  "SEAM:CHESS@cabinet": {
   "file": "arcade/chess/index.html",
   "tag": "SEAM:CHESS",
   "purpose": "chess cabinet: proven 0x88 core, tiered brain, THE Hand twice on the 3D table, hold-to-move + orbit"
  },
  "SEAM:CHESS@arcade-hub": {
   "file": "arcade/index.html",
   "tag": "SEAM:CHESS",
   "purpose": "cabinet 05 card + board tab"
  },
  "SEAM:ENDGAME@claw": {
   "file": "arcade/claw/index.html",
   "tag": "SEAM:ENDGAME",
   "purpose": "the prize counter door: token gate, treasury prize connect, claim tickets"
  },
  "SEAM:ENDGAME@chess-cabinet": {
   "file": "arcade/chess/index.html",
   "tag": "SEAM:ENDGAME",
   "purpose": "chess mints the key: three straight from the Hand reveals the treasury code"
  },
  "SEAM:DAILY_POV": {
   "file": "worker/src/index.js",
   "purpose": "doctrine as code: territories, tiered source registry, resist-list, stage prompts, momentum rubric, 12-slot edition template; consumed by pipeline/STUDIO/EXCAVATE"
  },
  "SEAM:DAILY_SPINE": {
   "file": "worker/src/index.js",
   "purpose": "lake-filler: CAPTURE 28 feeds + GDELT -> hash dedup -> embed -> FILTER (echo + t1 classify) -> CONNECT (clusters + mechanical momentum); runs before edition, failures never block publishing"
  },
  "SEAM:DAILY_COMPOSER": {
   "file": "worker/src/index.js",
   "purpose": "twelve from the lake: POV slot-filler (quotas + edge law) + six-format law + t3 INTERPRET/APPLY voice; legacy synthesis survives as fallback; shared publishEdition backlinks signals"
  },
  "SEAM:EXCAVATE": {
   "file": "worker/src/index.js",
   "purpose": "lake as recon surface: /excavate/lake semantic search (territory/tier/window filters) + /excavate/cluster (neighborhood + provenance thread to published DAILY stories); intelligence page feeds every synthesize from it"
  },
  "SEAM:DAILY_HEALTH": {
   "file": "worker/src/index.js",
   "purpose": "admin observability door: lake backlog depths, spine heartbeat, per-feed error history, edition provenance share, named flags"
  },
  "SEAM:EDITION_WATCHDOG": {
   "file": "worker/src/index.js",
   "purpose": "the alarm that did not exist: 06:00 ground-truth check on the edition row, not on a return value; DARK when nothing reached status=published or a row stalled at building, LEGACY when no item carried a signal_id and the fallback carried the paper; mails over Resend to ALERT_EMAIL and speaks even when it cannot read the lake \u2014 health that only an admin JWT can see is how DAILY went dark for seven days"
  },
  "SEAM:PROMOTE": {
   "file": "worker/src/index.js",
   "purpose": "how the lake grows by hand: /excavate/promote hashes on the capture fingerprint, takes an analyst-assigned tier, enters at raw and rides the newest-first drain into embed/filter/connect; momentum.promoted carries who, when, provider and the query that surfaced it, merged (never replaced) by every downstream stage"
  },
  "SEAM:VOICE@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:VOICE",
   "purpose": "the one measured thing on the search surface: a voice strip above the result cards leading with how much of a brand's name on social is actually the brand \u2014 kool-aid reads 97 mentions, ~12 about the beverage, 87% somebody else's idiom. Everything else on that page was position: confidence was the array index and the card bar was width 50+i*8, both now gone. Fails silent \u2014 no provider, no session, no posts and the strip does not draw"
  },
  "SEAM:VOICE": {
   "file": "worker/src/index.js",
   "purpose": "the fourth rail \u2014 what people actually said. Consumer voice is AGGREGATE, not signal: one post is noise, a thousand is a measurement, so this never writes rows to the lake (FILTER does 12 a slice and hashInput cannot dedupe a repost) and returns ONE row per entity per window \u2014 mentions, authors, engagement, trend vs our own KV history, themes and sentiment from one batched t1 call on a sample. Bluesky because in 2026 no free door remains: Reddit's unauthenticated .json 403s since May 2026 and its commercial floor is $12k/year, X is $200/mo minimum, TikTok and Instagram have no honest door. searchPosts is not public despite the docs, so a session is required \u2014 free, KV-cached 90m. Counts are arithmetic; only the reading goes to a model"
  },
  "SEAM:PROPOSE": {
   "file": "worker/src/index.js",
   "purpose": "the lake writes its own themes: recurring clusters (weeks touched, span, breadth, source count, paper provenance) go to t3 for a thesis in the DAILY voice \u2014 one batched call, KV-cached 24h; stat and bar are computed from recurrenceRollup and never written by the model, every card declares provenance:lake, and nothing publishes itself. SEAM:PROMOTE run the other direction: there a human hands the lake a signal, here the lake hands a human a theme"
  },
  "SEAM:RECURRENCE": {
   "file": "worker/src/index.js",
   "purpose": "the lake's long memory: recurring themes ranked by weeks touched, span, breadth and paper provenance; cluster pulse on the cluster door"
  },
  "SEAM:TELEMETRY@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:TELEMETRY",
   "purpose": "the featured surface drinks from its own lake: every curated card already carried the exact query it hands OpenAlex, so it hands the same string to /excavate/lake and gains a second evidence rail \u2014 public academic depth plus what Unsurfaced actually captured. The bar was `bar:84`, a literal the live path returned unchanged; it is now the mean momentum of the matching signals over the rubric's own published 0-30 ceiling, and no lake evidence draws no bar at all. Optional and silent: any failure leaves the public lenses untouched"
  },
  "SEAM:TELEMETRY": {
   "file": "daily/index.html",
   "purpose": "the face wears the engine: live lake counts off the public /daily/lake door as the masthead marquee"
  },
  "SEAM:ARCADE_ECON": {
   "file": "arcade/pop-a-shot/index.html",
   "purpose": "run-it-back law: revealOk restart for joined players, fresh session token minted on next round start, arcade return door on the score card"
  },
  "SEAM:APPROVAL@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:APPROVAL",
   "purpose": "the /whoami door reports approval, not just role \u2014 one read of app_user returns role and status, approved = status approved or admin, so any consumer gates on the same DB truth is_approved() enforces in RLS"
  },
  "SEAM:APPROVAL@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:APPROVAL",
   "purpose": "the gate before the platform \u2014 _authShowApp() checks is_approved() (same RPC shape as is_admin) and routes an unapproved profile to the pending screen instead of the hub; session stays alive so approval needs no second sign-up, and an rpc error fails closed to pending"
  },
  "SEAM:STUDY_ADMIN": {
   "file": "intelligence/index.html",
   "purpose": "admin edits any study: ownership never rewritten (row drops partner_id on edit), invites survive an editor that never hydrated them, reward locked once responses exist \u2014 the promise made to responders does not move"
  },
  "SEAM:STUDY_DELETE": {
   "file": "intelligence/index.html",
   "purpose": "two-tier teardown behind a confirm gate: no responses = hard delete (children cascade), responses = archive \u2014 the rows are receipts and receipts survive; RLS doors opened in 0016"
  },
  "SEAM:FREE_STUDY@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:FREE_STUDY",
   "purpose": "$0 studies are first-class: the pay field round-trips zero (was silently rewritten to $10), fund block and pay buttons never draw, boards read FREE, done screen makes no promise \u2014 free means no payment object, not a $0 one"
  },
  "SEAM:FREE_STUDY": {
   "file": "worker/src/index.js",
   "purpose": "the invite email tells the truth about zero: pay_cents rides the study read and a $0 study invites volunteers \u2014 subject and body drop the paid claim; pay/fund routes' amount<=0 rejections become backstops, not walls"
  },
  "SEAM:INTRO@daily": {
   "file": "daily/index.html",
   "tag": "SEAM:INTRO",
   "purpose": "the masthead reveal \u2014 type-motion only: Syne, the rule, the tag; no 3D, no video, the 5MB hand stays on the landing page where it earns its weight. Full sequence on the session's first open (sessionStorage), a short breathe after; reduced motion gets the lockup instantly"
  },
  "SEAM:GUEST_LINK": {
   "file": "worker/src/index.js",
   "purpose": "the public response door: link possession is the credential (the invite emails' own law); /mine/study reads live studies safely, /mine/respond takes free-study answers with email + ZIP through service role \u2014 the response_bi trigger nulls responder_id and skips the profile block so GUEST-#### and the ZIP segment survive; paid studies hard-reject here, one response per email per study by index, per-IP rate door; guest email never leaves the row \u2014 mine_study_responses does not select it"
  },
  "SEAM:GUEST_LINK@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:GUEST_LINK",
   "purpose": "the link and the room: Copy link on every live study's detail (pay-aware caption), and a boot hook riding the existing ?study= consumer \u2014 signed-out + free opens the guest overlay (media on the /media/ rail, questions, email + ZIP + consent), signed-in or paid falls through to the pending-study flow and the Stripe rail untouched"
  },
  "SEAM:MINE_SCALE": {
   "file": "worker/src/index.js",
   "purpose": "the competitive pass: screeners enforced where the pass keys live (guests never see them), milestone mail at 1/10/25/50 and at target via the auth admin door, target_n auto-close in one service-role place, and /s/:id \u2014 the share link unfurls as the study, not a homepage"
  },
  "SEAM:MINE_SCALE@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:MINE_SCALE",
   "purpose": "builder grows screeners (qualifying answers) and a response target with live progress; a disqualifying answer is a polite exit, not a stored row; every finishing guest meets the panel loop \u2014 join with the email that already answered; The Read prints to a branded report a partner can forward"
  },
  "SEAM:MINE_LAKE": {
   "file": "worker/src/index.js",
   "purpose": "primary research becomes signal: one digest row per study (VOICE law \u2014 aggregate, never per-post noise) enters through the PROMOTE machinery at raw, embedded on the next drain slice, tier 1 because nothing outranks what real people actually said; verbatims ride as anon labels only \u2014 no emails, ZIPs, or profile fields ever leave the response table"
  },
  "SEAM:MINE_LAKE@intelligence": {
   "file": "intelligence/index.html",
   "tag": "SEAM:MINE_LAKE",
   "purpose": "one click on the study detail sends the read and verbatims into the lake \u2014 after the next slice, EXCAVATE answers with what the culture is saying and what our own respondents told us in the same result set"
  },
  "SEAM:LIVE_EDIT": {
   "file": "intelligence/index.html",
   "purpose": "clients optimize mid-flight: partners edit live studies \u2014 adds and wording apply to new responders while collected answers keep their receipts; question ids are stable (PATCH in place, never delete-all-reinsert, which minted new ids and orphaned every answer), types and removals lock once responses exist, and a zero-row PATCH surfaces instead of no-oping (RLS door: 0019)"
  },
  "SEAM:FIELD_STATE": {
   "file": "worker/src/index.js",
   "purpose": "cluster states from pure arithmetic; EMERGING/ACCELERATING/STRUCTURAL/COOLING/CONTESTED/STEADY + curve shapes; geometry pass feeds CONTESTED"
  },
  "SEAM:BOOK_ANCHOR": {
   "file": "worker/src/index.js",
   "purpose": "relevance gate; book_anchors as passage-side vectors, PROPOSE re-ranks at 0.6 + 0.4*relevance; /excavate/anchors admin door (0020)"
  },
  "SEAM:SCOREBOARD": {
   "file": "worker/src/index.js",
   "purpose": "every EMERGING/ACCELERATING read logged to cluster_calls; resolved >30d as converted/held/faded (0020)"
  },
  "SEAM:ARRIVAL_LIVE": {
   "file": "intelligence/index.html",
   "purpose": "arrival consumes PROPOSE; the read + five-state strip + receipts; below 4 cards the 32-topic pool paints unchanged"
  },
  "SEAM:FIELD_RAIL": {
   "file": "worker/src/index.js",
   "purpose": "tokenized invites \u2014 the paid fielding rail; mint/list/revoke, token study door, token respond burns the token; panel of 3 made this the only way to field"
  },
  "SEAM:FIELD_RAIL@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:FIELD_RAIL",
   "purpose": "?t= token door reusing the guest overlay; invited email locked, no ZIP, response timed for the quality scan"
  },
  "SEAM:RESPONSE_QUALITY": {
   "file": "worker/src/index.js",
   "purpose": "pure qualityScan \u2014 speeder, straightline, thin_open, attention_fail, incomplete; flags prompt review, only admin 'rejected' leaves the read (0021)"
  },
  "SEAM:CLIENT_LENS": {
   "file": "worker/src/index.js",
   "purpose": "study_client read-only grant + aggregateResponses/crossTab with a 25-response floor; client reads never select email or ZIP (0021)"
  },
  "SEAM:CLIENT_LENS@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:CLIENT_LENS",
   "purpose": "client home + live dashboard; floor explained in client language, bars, verbatims as anon labels, cross-tab selector, 60s poll; client-access grant lane in study detail"
  },
  "SEAM:RESPONSE_QUALITY@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:RESPONSE_QUALITY",
   "purpose": "review lane on response cards \u2014 flags visible, admin clean/reject verdicts and the withdrawal delete, every write asserted by returned row count (RLS silent no-op law)"
  },
  "SEAM:CAM_CORE": {
   "file": "arcade/rps/index.html",
   "purpose": "unified camera stack: tasks-vision 1.0.0 (one revert constant), GPU delegate, warmup inference, rvfc per-camera-frame loops, visibility pause; RPS adds 640x480 ideal constraints + 3-of-4 stable read (~100ms lock)"
  },
  "SEAM:CAM_CORE@pop": {
   "file": "arcade/pop-a-shot/index.html",
   "tag": "SEAM:CAM_CORE",
   "purpose": "same stack; shooting physics untouched \u2014 rvfc gate wraps the existing 30Hz throttle as fallback"
  },
  "SEAM:CAM_CORE@thumb": {
   "file": "arcade/thumb/index.html",
   "tag": "SEAM:CAM_CORE",
   "purpose": "same stack + two kills: landmarker reused across toggles (WASM leak) and the boot-time setTimeout(33) eternal chain replaced by a gated loop; edge velocities EMA'd in the pure translator, pose stays raw"
  },
  "SEAM:STIMULUS": {
   "file": "intelligence/index.html",
   "purpose": "assets become stimuli: universal renderer (_assetKind/_assetHtml) \u2014 image/video/audio/html by filename; HTML renders live + clickable in iframe sandbox=allow-scripts allow-forms allow-popups, never allow-same-origin; per-question attach in the builder, pointers survive save/persist/load (0022)"
  },
  "SEAM:STIMULUS@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:STIMULUS",
   "purpose": "all three respondent doors select asset_key/asset_name; serveMedia adds CSP sandbox header to text/html so a directly-opened mock landing page still runs with an opaque origin"
  },
  "SEAM:CLICKPATH": {
   "file": "worker/src/index.js",
   "purpose": "behavior beside stated response: CLICK_BEACON appended to served HTML at serve time (never spliced, range-safe), cleanClicks total sanitizer (200-event budget, hostile-proof), clickSummary first-click distribution; all three response writes store clicks (0023)"
  },
  "SEAM:CLICKPATH@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:CLICKPATH",
   "purpose": "one message listener routes beacon events by contentWindow identity into the active response store; guest/token/responder flows arm and carry; client dashboard behavior block (first click = the headline) + admin interaction chip"
  },
  "SEAM:EVOLUTION_1": {
   "file": "intelligence/index.html",
   "purpose": "usability pass, all roles: consent copy names behavior capture; progress counter (answered/total, sticky); tracked full-screen (expand-in-place, beacon never severed); draft preview (real overlay, records nothing, submit walled); CSV import (quoted-name aware, direct object mint); response CSV export (proper quoting); reminder relabel; study duplication (fresh ids, pointers carried); client report download (floor-gated, house-marked)"
  },
  "SEAM:EVOLUTION_1@worker": {
   "file": "worker/src/index.js",
   "tag": "SEAM:EVOLUTION_1",
   "purpose": "consent version 2026-08-behavior (new words, new version); milestone mail reaches granted clients at exactly the floor crossing and the target \u2014 the two moments the results room is most worth opening"
  },
  "SEAM:DEEP_RAIL": {
   "file": "worker/src/index.js",
   "purpose": "the /deep route the page has called since birth: house PPLX key preferred with BYOK fallback, global + per-IP daily budget in KV answering 429, 6h response cache by payload hash (hits cost no budget), upstream status mirrored because the page's 401/402/429 copy branches on it, errors never cached"
  },
  "SEAM:INSIGHT_COMPILER": {
   "file": "worker/src/index.js",
   "purpose": "synthesize report mode: THE READ two-liner (reframe then move), per-insight implication, anti-metadata law in the prompt (never restate counts as findings, name disagreements), EARNED confidence from evidence density per category (3+ High / 2 Medium / 1 Low) \u2014 never hardcoded, never the model's self-grade"
  },
  "SEAM:INSIGHT_COMPILER@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:INSIGHT_COMPILER",
   "purpose": "reports complete on DATA not animation (bar parks at 94 until corpus fetched AND compiled); corpus flows through /excavate/synthesize; honest fallback ships as RETRIEVAL SUMMARY with confidence demoted to Unverified; padding loop dead (empty lens says so once at Low); Signal-Signal doubling dead; truth chips \u2014 audience panel and brand trend lines say MODELED until EVO-3 computes them from the lake"
  },
  "SEAM:MOAT_1": {
   "file": "worker/src/index.js",
   "purpose": "first computed brand number: /excavate/brand-signal \u2014 pure computeBrandSignal over lake matches (90d mentions, 30d-vs-prior momentum with zero-division sentinel, tier-1 share, leading signals); under 3 real matches (sim>=.3) returns thin:true \u2014 the moat fills honestly or not at all"
  },
  "SEAM:MOAT_1@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:MOAT_1",
   "purpose": "LAKE SIGNAL block above the modeled chart on brand dashboards, badged COMPUTED \u2014 not modeled, with its timestamp; thin lake says 'not enough to measure yet'; failures render nothing; both excavateBrand branches fire the load"
  },
  "SEAM:DASH_READ": {
   "file": "intelligence/index.html",
   "purpose": "EVO-4 opener: THE READ compiled onto dashboards from their own evidence via report-mode synthesize (lake auto-joins); thin corpus silent, honest-fallback cards never feed the compiler, failure renders nothing; roadmapped rationale captions on every modeled module (retires-as register) + method line inside the Lake block"
  },
  "SEAM:FLIGHT_RECORDER": {
   "file": "intelligence/index.html",
   "purpose": "the platform reports its own failures: crash bar paints any uncaught error/rejection with message+line, loading screen carries CANCEL (no scroll-locked dead ends), stage beacons + 25s watchdog name a stalled search, 3s SCRIPTS ARMED boot chip whose absence convicts a boot crash \u2014 born from a freeze that gave no testimony"
  },
  "SEAM:LAKE_LOOP": {
   "file": "worker/src/index.js",
   "purpose": "the organism breathes: /mine/publish-signal sends floor-cleared MINE aggregates into the lake as tier-0 signal (admin-gated, idempotent via content_hash mine-{study_id}, status filtered, raw doc embedding per the book); EXCAVATE brand signal and DASH_READ can now cite Unsurfaced's own field work"
  },
  "SEAM:LAKE_LOOP@arrival": {
   "file": "intelligence/index.html",
   "tag": "SEAM:LAKE_LOOP",
   "purpose": "admin Publish-to-lake button on study detail; worker enforces every law, the button asks and reports"
  }
 },
 "planned": [
  "SEAM:KNOWLEDGE_INGEST",
  "SEAM:OPS_AUTH",
  "SEAM:GUARD_SYNC",
  "SEAM:PROMPT_SYNC"
 ],
 "note": "planned seams live in /worker; they enter registry as they ship"
}
